HELP_TOPICS = {
    "tui_shell.overview": (
        "TUI shell provides a dependency-free terminal dashboard. It reuses view "
        "models and command palette data so terminal, CLI, and Tkinter views stay aligned."
    )
}

