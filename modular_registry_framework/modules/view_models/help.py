HELP_TOPICS = {
    "view_models.overview": (
        "View models provide shared display data for CLI, TUI, and Tkinter surfaces "
        "so each interface shows the same modules, health, commands, and artifacts."
    )
}

