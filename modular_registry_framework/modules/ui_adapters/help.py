HELP_TOPICS = {
    "ui_adapters": """
# UI Adapters

UI adapters explain how one registered capability can appear in several surfaces:
Tkinter screens, TUI panels, CLI commands, dashboard widgets, and reports.
Use this module to check whether a feature is visible everywhere it should be.
""".strip()
}

