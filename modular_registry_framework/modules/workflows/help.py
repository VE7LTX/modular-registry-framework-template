HELP_TOPICS = {
    "workflows": """
# Workflows

Workflows describe repeatable chains of module commands and events. They are the
bridge between small reusable modules and real application behavior such as
importing a file, storing records, generating a report, exporting data, and
auditing every step.
""".strip()
}

