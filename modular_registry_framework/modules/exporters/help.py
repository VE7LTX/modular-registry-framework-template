HELP_TOPICS = {
    "exporters.overview": (
        "Exporters are the output-side match for importers. They register reusable "
        "format handlers for Markdown, HTML/text, CSV, JSON, JSONL, XML, YAML, and YML."
    )
}

