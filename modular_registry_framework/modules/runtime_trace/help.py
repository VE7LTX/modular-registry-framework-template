HELP_TOPICS = {
    "runtime_trace.overview": (
        "Runtime tracing links jobs, imports, artifacts, reports, logs, and events "
        "with a shared trace_id so one workflow can be followed end to end."
    )
}

