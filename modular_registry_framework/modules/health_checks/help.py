HELP_TOPICS = {
    "health_checks.overview": (
        "Health checks answer whether the app is ready to run. Modules can register "
        "checks for configuration, paths, storage, API connectivity, dependencies, "
        "and traceability coverage."
    )
}

