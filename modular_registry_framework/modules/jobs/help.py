HELP_TOPICS = {
    "jobs.overview": (
        "The jobs module gives long-running or important work a common lifecycle: "
        "started, completed, or failed. It is useful for imports, transcription, "
        "API syncs, benchmark runs, exports, and report generation."
    )
}

