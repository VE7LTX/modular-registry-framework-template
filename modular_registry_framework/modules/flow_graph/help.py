HELP_TOPICS = {
    "flow_graph.overview": (
        "The flow graph module builds an automatic map of modules, services, screens, "
        "settings, importers, events, report sections, declared input/output ports, "
        "and explicit data-flow edges."
    ),
    "flow_graph.ports": (
        "Modules can call registry.add_data_input(), registry.add_data_output(), and "
        "registry.add_flow() to make their inputs, outputs, and transformations traceable."
    ),
}

