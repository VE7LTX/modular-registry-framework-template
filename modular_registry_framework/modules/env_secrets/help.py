HELP_TOPICS = {
    "env_secrets.overview": (
        "Environment and secrets support validates required keys without exposing "
        "actual values. It can load a local .env file, produce redacted diagnostics, "
        "and generate .env.example content."
    )
}

