HELP_TOPICS = {
    "workspace_scanner.overview": (
        "Workspace scanner inventories local project folders and detects common "
        "signals: git repos, READMEs, Python files, Markdown docs, SQLite files, "
        ".env files, tests, and logs."
    )
}

