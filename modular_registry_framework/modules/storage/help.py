HELP_TOPICS = {
    "storage.overview": (
        "Storage provides a small SQLite lifecycle baseline: connect, initialize, "
        "health-check, and backup. Feature modules can build repositories on top."
    )
}

