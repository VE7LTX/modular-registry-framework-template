HELP_TOPICS = {
    "audit_log.overview": (
        "The audit log listens to registry events and keeps a recent in-memory "
        "history of imports, reports, artifact creation, jobs, and user actions."
    )
}

