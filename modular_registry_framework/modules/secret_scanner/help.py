HELP_TOPICS = {
    "secret_scanner.overview": (
        "Secret scanner looks for likely hardcoded API keys, tokens, secrets, "
        "passwords, and bearer tokens while redacting values in output."
    )
}

