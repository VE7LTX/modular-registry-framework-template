HELP_TOPICS = {
    "settings_editor.overview": (
        "Settings editor provides shared CLI/TUI/Tkinter-friendly settings editing "
        "with JSON-aware value parsing and multi-format saves through Settings."
    )
}

