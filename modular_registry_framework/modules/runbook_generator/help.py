HELP_TOPICS = {
    "runbook_generator.overview": (
        "Runbook generator creates operational documentation from registered modules, "
        "settings, health checks, run commands, test commands, and data locations."
    )
}

