HELP_TOPICS = {
    "settings_manager.overview": (
        "The settings manager shows registered settings, their defaults, current "
        "values, labels, and help text. Modules own the settings they register, "
        "while the app stores selected values in JSON, JSONL, XML, YAML, or YML."
    )
}

